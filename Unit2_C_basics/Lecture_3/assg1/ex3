#include <stdio.h>
int main( )
{
	int a,b,sum ;
	printf("enter 2 numbers:");
	fflush(stdout);
	scanf("%d %d", &a, &b);
	fflush(stdin);
	sum=a+b ;
	printf("sum = %d",sum);
	fflush(stdout);
	return 0 ;

}
