#include <stdio.h>
#include <stdlib.h>

int main(void) {
	int i , n , sum= 0 ;

	printf("Enter an Integer:");
	fflush(stdout);
	scanf("%d",&n) ;
	for (i=0;i<=n;i++)
		sum=sum+i ;
	printf("sum = %d",sum);
	fflush(stdout);
	return 0 ;
}
